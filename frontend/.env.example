# Frontend environment variables
# Copy to .env.local and adjust if needed

# Django API base URL (used when NOT using the Vite proxy)
# If running via `npm run dev` the proxy in vite.config.ts handles this automatically.
VITE_API_URL=http://127.0.0.1:8000/api
